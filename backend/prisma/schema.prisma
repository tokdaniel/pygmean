generator photon {
  provider = "photonjs"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:prisma@mysql:3306/prisma"
}

model User {
  id       String  @default(cuid()) @id
  email    String  @unique
  password String
  name     String?
}
